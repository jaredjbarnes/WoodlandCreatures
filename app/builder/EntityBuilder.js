BASE.require([
    "jQuery"
], function () {
    BASE.namespace("app.builder");

    app.builder.EntityBuilder = function (elem, tags, services) {
        var self = this;
        var $elem = $(elem);
        
    };
});